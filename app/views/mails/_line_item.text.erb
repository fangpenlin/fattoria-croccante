<%= line_item_counter + 1 %>      <%= line_item.description %>      <%= line_item.qty %> × <%=  line_item.unit %>     <%= number_to_currency(line_item.price.amount) %>   <%= number_to_currency(line_item.total.amount) %>
